# Cloudflare Pages Redirects Configuration

# API 代理到后端
/api/* https://apimail.yzcjwds.xyz/api/:splat 200
/admin/* https://apimail.yzcjwds.xyz/admin/:splat 200
/telegram/* https://apimail.yzcjwds.xyz/telegram/:splat 200

# SPA Fallback - All routes should serve index.html for client-side routing
/*    /index.html   200

# Legacy URL Redirects (if migrating from old system)
# /old-path/*  /new-path/:splat  301

# Force HTTPS (handled by Cloudflare by default, but can be explicit)
# http://example.com/*  https://example.com/:splat  301

# Redirect common typos or alternative domains
# www.example.com/*  https://example.com/:splat  301
