# Ensure optional dependencies are installed
optional=true

# Use exact versions for better reproducibility
save-exact=false

# Disable package-lock for better compatibility
package-lock=true

# Enable legacy peer deps to avoid conflicts
legacy-peer-deps=false
